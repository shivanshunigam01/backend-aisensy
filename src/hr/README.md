# Zentroverse HR module
#
# Integrated API (used by create-space): `src/hr` mounted at **/api/hr**
# Full PeopleFlow reference package: `./hr` (TypeScript Express API)
#
# Collections use `hr_*` prefixes to avoid colliding with CRM / site models.
